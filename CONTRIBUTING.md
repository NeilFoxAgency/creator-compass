# Contributing

1. Fork & clone
2. pnpm install
3. cp .env.example .dev.vars
4. pnpm dev

Run `pnpm typecheck && pnpm test && pnpm build` before PRs.

Focus on: creator discovery tools, better fallbacks, accessibility, documentation, tests for new territories.

MIT licensed. No spam tools.
